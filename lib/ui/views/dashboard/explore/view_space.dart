import 'package:flutter/material.dart';
import 'package:hashnode_hasura_hackathon/ui/views/dashboard/explore/explore_view_model.dart';
import 'package:stacked/stacked.dart';

class ViewSpaceView extends ViewModelWidget<ExploreViewModel> {

  @override
  Widget build(BuildContext context, ExploreViewModel viewModel) {
    return Scaffold(
        backgroundColor: Colors.white,
        body: ListView(
          children: [
            
          ],
        ),
      );
  }
}
